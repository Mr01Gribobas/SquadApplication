<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SquadApplication.ViewCustom.ParticipantsPage"
             Title="ParticipantsPage"
             BackgroundImageSource="hkauthorize.jpg"
             >
    <StackLayout Padding="5" HorizontalOptions="Center" VerticalOptions="Start">
        <Label Text="Участники" FontSize="24" HorizontalOptions="Center" VerticalOptions="Start"/>
        <ScrollView>     
            
        <CollectionView x:Name="ParticipantsLisyViewItem"  
                        Background="Transparent"
                        ItemsSource="{Binding Users}"
                        VerticalOptions="Start"
                        HorizontalOptions="Center"
                        >
                <CollectionView.EmptyView>
                    <Label 
                        Text="Список пока пуст !!"
                        TextColor="Gray"
                        BackgroundColor="Transparent"
                        FontSize="24"
                        HorizontalTextAlignment="Center"
                        VerticalTextAlignment="Center"/>
                </CollectionView.EmptyView>                       
            <CollectionView.ItemTemplate>
                <DataTemplate>                    
                    <Border
                        Stroke="DarkKhaki"
                        StrokeShape="1"                        
                        >
                        <StackLayout
                            VerticalOptions="Center"
                            HorizontalOptions="Center"                      
                            >
                                <Label Text="{Binding Path=_userName}" TextColor="White"/>
                                <Label Text="{Binding Path=_callSing}" TextColor="White"/>
                                <Label Text="{Binding Path=_role}" TextColor="White"/>
                                <Label x:Name="isStaffed" Text="{Binding Path=_isStaffed}" TextColor="White"/>
                            </StackLayout>
                    </Border>                    
                </DataTemplate>
            </CollectionView.ItemTemplate> 
        </CollectionView>
        </ScrollView>
        <Button Text="GoBack"></Button>
    </StackLayout>
</ContentPage>